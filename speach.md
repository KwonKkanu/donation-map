# donation-map 발표 정리

## 1. 프로젝트 주제

저희 프로젝트의 주제는 **기부 캠페인 통합 검색 서비스**입니다.

여러 기부 플랫폼에 흩어져 있는 캠페인 정보를 한 곳에 모으고, 사용자가 원하는 기부 대상을 더 쉽게 찾을 수 있도록 검색과 필터 기능을 제공하는 웹 서비스입니다.

현재는 Kakao Together, GoodNeighbors, Naver Happybean의 공개 캠페인 데이터를 수집하고, 이를 정제해서 카드 형태로 보여줍니다.

핵심은 단순히 캠페인을 나열하는 것이 아니라, 각 캠페인을 다음 기준으로 분류하는 것입니다.

- 누구를 돕는가: `target`
- 무엇을 지원하는가: `supportType`
- 어떤 키워드로 찾을 수 있는가: `tags`

예를 들어 어떤 캠페인이 “독거 어르신께 식사를 지원하는 캠페인”이라면, `노인`, `식사지원`, `#어르신`, `#독거`, `#식사지원`처럼 정리됩니다.

## 2. 프로젝트 주제 선정 이유

기부 캠페인은 여러 플랫폼에 나뉘어 있고, 각 플랫폼마다 정보 구조와 표현 방식이 다릅니다.

사용자는 특정 플랫폼을 하나씩 들어가서 캠페인을 찾아야 하고, 원하는 기부 대상을 기준으로 비교하기도 어렵습니다.

예를 들어 사용자가 “동물 치료비 모금”이나 “노인 식사지원” 같은 캠페인을 찾고 싶어도, 플랫폼마다 검색 방식과 분류 기준이 달라서 한눈에 보기 어렵습니다.

그래서 저희는 여러 플랫폼의 캠페인을 한 곳에 모으고, 공통된 기준으로 정리하면 더 편리한 기부 탐색이 가능하다고 생각했습니다.

이 프로젝트는 “기부를 하고 싶은 마음은 있지만 어디에 어떻게 기부해야 할지 모르는 사용자”가 더 쉽게 캠페인을 찾도록 돕는 것을 목표로 합니다.

## 3. 팀원 별 역할

현재 정리 기준으로 역할은 다음과 같이 나눌 수 있습니다.

- 데이터 수집 담당
  - Kakao Together, GoodNeighbors, Naver Happybean 캠페인 수집 구조 확인
  - 플랫폼별 원본 데이터 수집
  - `out/*.json` 생성

- 데이터 정제 담당
  - 플랫폼마다 다른 데이터 구조를 공통 구조로 변환
  - `processed/campaigns.json` 생성
  - Ollama LLM을 활용한 캠페인 요약, 대상 분류, 지원 유형 분류, 태그 생성
  - LLM 결과 검증 및 규칙 기반 보정

- 프론트엔드 담당
  - 캠페인 카드 UI 구현
  - 검색창, 대상 필터, 지원 유형 필터, 플랫폼 필터 구현
  - 태그 chip 표시 및 클릭 검색 구현

- 서버/배포 담당
  - Node.js 서버 구성
  - `/api/campaigns` API 제공
  - GitHub/Railway 배포 구조 확인
  - 자동 갱신 방향 검토

실제 발표에서는 팀원 이름에 맞춰 위 역할을 다시 매칭하면 됩니다.

## 4. 사용 기술 스택

이번 프로젝트에서 사용한 주요 기술은 다음과 같습니다.

- JavaScript / Node.js
  - 크롤링 CLI
  - 데이터 가공 스크립트
  - 간단한 HTTP 서버

- HTML / Tailwind CSS
  - 캠페인 검색 화면 UI
  - 카드형 레이아웃
  - 필터 UI 구성

- Ollama
  - 로컬 LLM 실행
  - 캠페인 요약, 대상 분류, 지원 유형 분류, 태그 생성

- JSON
  - 원본 크롤링 데이터 저장
  - 가공된 캠페인 데이터 저장
  - LLM 정제 cache 저장

- Git / GitHub
  - 프로젝트 버전 관리
  - 배포 연동

- Railway
  - 웹 서비스 배포 확인

## 5. 프로젝트 주요 기능

### 5.1 캠페인 데이터 수집

세 개의 기부 플랫폼에서 공개 캠페인 데이터를 수집합니다.

- Kakao Together
- GoodNeighbors
- Naver Happybean

수집된 데이터는 `out/` 폴더에 JSON 형태로 저장됩니다.

### 5.2 데이터 정규화

각 플랫폼의 데이터 구조가 다르기 때문에, 이를 공통 캠페인 구조로 변환합니다.

공통 구조에는 다음 정보가 포함됩니다.

- 제목
- 단체명
- 요약
- 플랫폼
- 링크
- 이미지
- 모금액
- 목표액
- 진행률
- 원본 데이터

### 5.3 LLM 기반 데이터 정제

Ollama 로컬 LLM을 활용해 캠페인 정보를 정제합니다.

정제 결과에는 다음 값이 포함됩니다.

- `oneLineSummary`: 한 줄 요약
- `target`: 누구를 돕는가
- `supportType`: 무엇을 지원하는가
- `tags`: 검색용 태그

### 5.4 규칙 기반 보정

LLM 결과가 항상 정확하지는 않기 때문에, 명확한 키워드가 있는 경우 규칙 기반으로 보정합니다.

예를 들어:

- `어르신`, `독거`, `노인` → `노인`
- `장애`, `시각장애`, `발달장애` → `장애인`
- `유기견`, `유기묘`, `구조묘` → `동물`
- `치료`, `수술`, `의료` → `의료지원`
- `식사`, `김치`, `도시락` → `식사지원`

이 과정을 통해 LLM이 한쪽 카테고리로 쏠리는 문제를 줄였습니다.

### 5.5 검색 및 필터 UI

사용자는 캠페인을 다음 기준으로 탐색할 수 있습니다.

- 검색어
- 대상
- 지원 유형
- 플랫폼
- 해시태그

또한 카드 상단에 있는 `#태그`를 클릭하면 해당 태그로 바로 검색할 수 있습니다.

예를 들어 `#어르신` 태그를 클릭하면 어르신 관련 캠페인만 필터링됩니다.

## 6. 현재까지 구현한 내용

현재까지 구현된 내용은 다음과 같습니다.

- 3개 플랫폼 크롤링 구현
- `out/*.json` 원본 데이터 생성
- 공통 데이터 구조 변환
- Ollama LLM 정제 연동
- LLM 정제 결과 cache 저장
- `target/supportType/tags` 분류 구조 구현
- `titleRaw` 기반 원본 제목 유지
- `category = target` 호환 정책 적용
- 규칙 기반 분류 보정 구현
- `processed/campaigns.json` 생성
- 캠페인 카드 UI 구현
- 대상/지원 유형/플랫폼 필터 구현
- `#태그` 검색 구현
- 태그 chip 클릭 검색 구현

현재 데이터 기준 품질은 다음과 같습니다.

- 전체 캠페인 수: 1336개
- `target` 기타 비율: 0.67%
- `supportType` 기타 비율: 2.84%
- tags 빈 배열 비율: 1.80%
- 제목 원본 유지 실패: 0개
- `category`와 `target` 불일치: 0개

## 7. 어려웠던 점

### 7.1 플랫폼마다 다른 데이터 구조

각 기부 플랫폼은 캠페인을 표현하는 방식이 다릅니다.

어떤 플랫폼은 요약이 잘 정리되어 있고, 어떤 플랫폼은 카테고리 정보가 부족하거나 숫자 코드로만 제공됩니다.

그래서 단순히 데이터를 모으는 것보다, 공통 구조로 정리하는 과정이 중요했습니다.

### 7.2 LLM 분류 품질 문제

처음에는 LLM이 category를 자유롭게 생성해서 UI 필터로 쓰기 어려웠습니다.

또 어떤 경우에는 제목을 임의로 바꾸거나, 대상 분류를 잘못 판단하는 문제가 있었습니다.

이를 해결하기 위해:

- 허용된 target/supportType 목록을 만들고
- LLM 출력값을 검증하고
- 목록 밖 값은 보정하고
- 명확한 키워드는 규칙 기반으로 다시 판단하도록 했습니다.

### 7.3 Ollama 실행 환경 문제

Ollama가 설치되어 있지 않거나, 서버가 실행되지 않으면 LLM 정제가 수행되지 않았습니다.

그 결과 모든 캠페인이 `기타`로 분류되고 tags가 비어 있는 문제가 있었습니다.

이를 해결하기 위해 Ollama 설치, 모델 다운로드, 서버 연결 확인, 강제 재정제를 순서대로 진행했습니다.

### 7.4 태그의 역할 정의

처음에는 tags가 단순히 카드 하단에 표시되는 보조 정보에 가까웠습니다.

하지만 실제로는 사용자가 `#고양이`, `#치료비`, `#모금`처럼 검색하고 싶어 할 수 있기 때문에, tags를 검색 진입점으로 활용하도록 UI를 수정했습니다.

## 8. 후기

이번 프로젝트를 진행하면서 단순히 데이터를 수집하는 것보다, 수집한 데이터를 사용자가 이해하고 검색할 수 있는 형태로 정리하는 것이 더 중요하다는 것을 느꼈습니다.

LLM을 사용하면 자동으로 모든 분류가 해결될 것 같았지만, 실제로는 LLM 결과를 그대로 쓰기 어렵고, 검증과 후처리가 꼭 필요했습니다.

특히 기부 캠페인처럼 의미가 중요한 데이터는 잘못된 분류가 사용자 경험에 직접 영향을 주기 때문에, LLM과 규칙 기반 보정을 함께 사용하는 방식이 더 안정적이었습니다.

또한 UI에서도 데이터 구조가 제대로 드러나야 한다는 점을 배웠습니다.

데이터에 `target`, `supportType`, `tags`가 있어도 화면에서 이를 잘 보여주지 않으면 사용자는 개선된 데이터를 체감하기 어렵습니다.

## 9. 앞으로의 개선 방향

앞으로 개선할 수 있는 부분은 다음과 같습니다.

### 9.1 분류 품질 검수

현재 자동 분류 품질은 많이 개선되었지만, 일부 복합 캠페인은 여전히 애매할 수 있습니다.

샘플 캠페인을 수동으로 검수하고, 자주 틀리는 패턴을 규칙에 추가하면 품질을 더 높일 수 있습니다.

### 9.2 태그 추천 영역 추가

현재는 카드의 태그를 클릭해서 검색할 수 있습니다.

추가로 많이 등장하는 태그를 상단에 인기 태그처럼 보여주면 사용자가 더 쉽게 탐색할 수 있습니다.

예:

- `#어르신`
- `#치료비`
- `#유기묘`
- `#식사지원`
- `#주거지원`

### 9.3 자동 갱신

현재는 수동으로 크롤링과 정제를 실행합니다.

앞으로는 1시간마다 자동으로:

1. 새 캠페인 크롤링
2. 변경된 캠페인만 LLM 정제
3. `processed/campaigns.json` 갱신
4. 배포 화면 반영

이 흐름을 만들 수 있습니다.

### 9.4 배포 안정화

Railway 배포 환경에서 최신 `processed/campaigns.json`과 UI가 정확히 반영되는지 확인해야 합니다.

Ollama를 Railway에서 직접 돌리는 것은 무거울 수 있기 때문에, 로컬 또는 별도 작업 환경에서 정제한 결과를 GitHub/Railway에 반영하는 방식도 고려할 수 있습니다.

### 9.5 상세 페이지 또는 즐겨찾기

현재는 카드에서 상세보기/후원하기 링크로 이동합니다.

앞으로는 서비스 내부에서 캠페인 상세 정보를 더 자세히 보여주거나, 관심 캠페인을 저장하는 기능도 추가할 수 있습니다.

## 10. 발표용 한 줄 요약

이 프로젝트는 여러 기부 플랫폼의 캠페인을 수집하고, LLM과 규칙 기반 보정을 통해 `대상`, `지원 유형`, `태그`로 정리하여 사용자가 원하는 기부 캠페인을 쉽게 검색하고 찾을 수 있게 만든 서비스입니다.
